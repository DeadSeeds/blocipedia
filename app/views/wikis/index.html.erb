<div class="container">

  <div class="row">
    <div class="col-md-11">
      <%= link_to "New Wiki", new_wiki_path, class: 'btn-group btn-success btn-sm' %>
    </div>
  </div>
  <div class="jumbotron">
  <center><h1>Blocipedia — Look stuff up!</h1></center>
  </div>

  <div class="row">
    <div class="col-md-11">
      <% @wikis.each do |wiki| %>
      <div class="media">
        <div class="media-body">
          <h4 class="media-heading">
            <% if @user.present? %>
              <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
            <% end %>
            <%= link_to wiki.title, wiki %>
          </h4>
        </div>
      </div>
      <% end %>
    </div>
  </div>
</div>
