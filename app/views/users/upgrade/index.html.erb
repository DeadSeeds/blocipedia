<div class="col-md-8">
  <div class="row">
    <div class="col-md-11">
      <%= link_to "Home", wikis_path, class: 'btn btn-info btn-sm' %>
    </div>
  </div>
  <div class="row">
    <h4>Upgrade your account for access to private wiki creation.</h4>
      <ul>
        <li>Store information for later use</li>
        <li>Have a record of your thought process</li>
        <li>Keep track of your plan to take over the world</li>
        <li>You currently have a <%= @role %> account</li>
      </ul>
  </div>
  <div class="row">
    <%= form_tag charges_path  %>
      <script class='stripe-button' src="https://checkout.stripe.com/checkout.js" data-key="<%= @stripe_btn_data[:key] %>" data-amount="<%= @stripe_btn_data[:amount] %>" data-description="<%= @stripe_btn_data[:description] %>" ></script>
  </div>
</div>
